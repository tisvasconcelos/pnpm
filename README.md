# [PNPN](https://pnpm.js.org/) monorepo / workspace

 - `pnpm add SHARED@wokspace` to add local shared lib or component
 - Using [tsdx](https://tsdx.io/) to create libs and components (`nppm tsdx create NAME`)